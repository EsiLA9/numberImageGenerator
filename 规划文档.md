# 序号图片生成器策划文档

## 1. 项目概述

### 1.1 项目名称

序号图片生成器（Number Image Generator）

### 1.2 项目目标

制作一个运行在浏览器中的轻量级图片辅助工具：用户上传一张已经制作好的背景图片，设置序号范围和序号样式，在画布中通过拖拽调整序号位置，然后批量生成带有不同序号的 PNG 图片。

最终以 GitHub Pages 静态网站形式部署，不依赖后端服务器。图片读取、绘制、导出和批量打包全部在用户浏览器本地完成。

### 1.3 使用场景

- 为一组阅读记录、课程资料、资料卡片或宣传图片添加 `01`、`02`、`03` 等辅助序号。
- 背景图的主体内容由用户或朋友提前制作好，工具只负责叠加数字序号。
- 一次生成连续编号的图片，避免手动修改和重复导出。

## 2. 已确认需求

以下内容为本项目的确定范围：

1. 序号必须支持鼠标或触摸拖拽定位。
2. 必须支持批量生成连续序号图片。
3. 背景图片由用户直接提供，背景中的标题、图标、Logo 等内容不拆分为可编辑图层。
4. 输出格式只需要 PNG。
5. 序号样式需要能够适配不同尺寸的背景图片。

参考图片仅用于说明视觉用途和布局关系，不作为需要重新编辑的图层模板。工具应将其视为一张完整背景图片。

## 3. 功能需求

### 3.1 背景图片输入

- 支持上传 PNG、JPG、JPEG、WebP 等常见图片格式。
- 读取并显示原始图片宽度、高度和比例。
- 预览区域可以缩放显示，但导出时使用原始图片分辨率。
- 支持替换当前背景图片。
- 图片只在浏览器本地处理，不上传到服务器。
- 对超大图片提供合理提示，避免浏览器因内存不足无响应。

### 3.2 序号范围

提供以下参数：

- 起始序号，默认 `1`。
- 结束序号，默认与起始序号相同或提供一个适合演示的范围。
- 补零位数：不补零、两位、三位或自定义位数。
- 可选前缀和后缀，默认为空。
- 实际显示示例，例如输入范围 `1–12`、两位补零时显示 `01`。

序号格式示例：

| 起始值 | 结束值 | 补零 | 结果示例 |
| ---: | ---: | ---: | --- |
| 1 | 3 | 2 位 | 01、02、03 |
| 7 | 9 | 不补零 | 7、8、9 |
| 1 | 3 | 3 位 | 001、002、003 |

需要校验起始值、结束值和数量上限。第一版建议限制单次生成数量，例如最多 500 张，并在超过限制时明确提示。

### 3.3 序号样式

序号至少支持：

- 字体族。
- 字号。
- 字体颜色。
- 常规、粗体、斜体等字形选项。
- 透明度。
- 水平对齐方式：左对齐、居中、右对齐。
- 垂直对齐方式：顶部、居中、底部。
- 描边颜色和描边宽度。
- 阴影颜色、模糊程度、偏移量。

字号、描边和阴影等参数应以“相对于原图尺寸”的方式保存或计算，使同一套配置可以应用到不同分辨率的背景图。

### 3.4 拖拽定位

- 预览画布中显示序号的可交互区域。
- 鼠标按下并拖动可以移动序号。
- 触摸设备支持拖拽。
- 拖拽过程中实时更新预览。
- 提供水平位置和垂直位置的数值或百分比显示，便于精确调整。
- 提供吸附到水平居中、垂直居中、四边安全边距等快捷操作。
- 序号位置应以原图坐标保存，而不是只保存屏幕像素坐标。
- 背景预览缩放或窗口尺寸变化时，序号在原图中的相对位置保持不变。

### 3.5 实时预览

- 默认预览当前序号。
- 可以切换预览序号，确认位数变化后的效果。
- 修改字体、颜色、位置、范围等参数后立即刷新。
- 预览中应区分“显示缩放比例”和“实际导出效果”。
- 当没有上传背景图片时显示引导状态，而不是显示空白画布。

### 3.6 批量生成

- 根据起始值和结束值生成全部序号图片。
- 每张图片使用同一背景图和同一套样式，只替换序号内容。
- 输出文件名支持模板，例如 `reading-{number}.png`。
- `{number}` 使用最终格式化后的序号，例如 `reading-01.png`。
- 生成前显示预计数量和文件名示例。
- 生成过程中显示进度。
- 用户可以取消尚未完成的批量任务。
- 生成完成后提供 ZIP 下载。
- ZIP 内只包含 PNG 文件，不产生 JPG 或其他格式。
- 小数量图片可提供单张下载，批量结果默认使用 ZIP。

## 4. 非功能需求

### 4.1 隐私与数据安全

- 不上传背景图片。
- 不调用图片处理后端。
- 不保存用户图片到服务器。
- 不需要账号、数据库或云存储。
- 如使用第三方字体或库，应尽量使用公开、稳定、可固定版本的资源；更稳妥的基础版本优先使用系统字体和本地浏览器能力。

### 4.2 兼容性

- 优先支持最新版 Chrome、Edge、Firefox、Safari。
- 支持 Windows 桌面端使用。
- 兼顾手机和平板触摸操作。
- 对不支持批量下载或文件系统能力的浏览器提供普通 ZIP 下载方案。

### 4.3 性能

- 预览时可使用缩小画布，导出时重新按原图尺寸绘制。
- 批量生成采用分批或逐张处理，避免一次性占满内存。
- 生成大量图片时及时释放临时 Blob 和画布资源。
- 默认限制批量数量，并在必要时提示用户降低图片尺寸或分批生成。

## 5. 页面与交互设计

### 5.1 页面布局

建议采用“控制面板 + 预览画布”的布局：

- 左侧或顶部：上传、序号设置、样式设置、导出设置。
- 右侧或下方：画布预览、预览序号切换、生成进度。
- 窄屏设备：控制面板与预览区上下排列。

### 5.2 推荐操作流程

1. 用户上传背景图片。
2. 页面显示原图尺寸，并自动给出适合的初始字号和位置。
3. 用户设置起始序号、结束序号和补零格式。
4. 用户调整字体、颜色及其他效果。
5. 用户在画布中拖拽序号到目标位置。
6. 用户切换几个不同序号检查显示效果。
7. 用户预览输出文件名和图片数量。
8. 点击“批量生成 PNG”。
9. 页面显示进度，完成后下载 ZIP。

### 5.3 防误操作

- 没有背景图片时禁用生成按钮。
- 起始值大于结束值时显示明确错误。
- 生成数量过大时要求调整范围或确认继续。
- 修改背景图片时提示当前配置是否继续沿用。
- 批量生成期间锁定会影响任务的参数，或提示取消后再修改。

## 6. 技术方案

### 6.1 总体架构

采用纯静态前端：

- `index.html`：页面结构。
- `style.css`：响应式界面和视觉样式。
- `app.js`：状态管理、交互、画布绘制和导出。
- `lib/`：可选的 ZIP 压缩库等前端依赖。

GitHub Pages 只负责提供静态文件。应用运行时不需要 Python、Node.js、Flask 或其他后端服务。

### 6.2 图片绘制

使用 HTML Canvas：

1. 通过 `URL.createObjectURL()` 或 `FileReader` 读取图片。
2. 使用 `Image` 加载背景图。
3. 预览画布按照容器尺寸缩放显示。
4. 导出时创建原始尺寸画布。
5. 绘制背景图。
6. 根据原图坐标绘制序号及其描边、阴影。
7. 使用 `canvas.toBlob('image/png')` 导出 PNG。

### 6.3 坐标与尺寸适配

核心原则是配置使用原图坐标或归一化比例，而不是直接使用预览画布像素。

建议保存：

- 序号中心点的 `xPercent` 和 `yPercent`；或
- 原图坐标 `x`、`y`，同时记录编辑时的原图尺寸。

字号建议保存为原图短边或高度的比例，再根据导出图尺寸计算实际字号。这样在不同尺寸背景上可以保持相近的视觉比例。

导出时还应考虑设备像素比，避免预览清晰但输出文字模糊。

### 6.4 批量导出

- 使用同一份配置对象循环生成不同序号。
- 每次生成一张 PNG Blob。
- 使用 ZIP 库将 Blob 加入压缩包。
- 每处理一张更新进度条。
- 完成后生成 ZIP Blob 并触发浏览器下载。
- 任务结束后释放对象 URL。

### 6.5 配置数据

第一版可以只保存在当前页面内存中，不要求账号或云端保存。后续可增加：

- 导出配置 JSON。
- 导入配置 JSON。
- 使用 `localStorage` 保存最近一次参数。

不建议在第一版自动持久化背景图片本身。

## 7. 项目目录建议

```text
projects/numberImageGenerator/
├─ index.html
├─ style.css
├─ app.js
├─ README.md
└─ lib/
   └─ jszip.min.js
```

如果后续需要使用 CDN，应同时设计无网络 CDN 时的降级提示；更稳妥的 GitHub Pages 版本可以将依赖固定在仓库内。

## 8. 开发阶段计划

### 阶段一：基础页面与单张预览

- 创建静态页面。
- 实现背景图片上传。
- 实现 Canvas 预览。
- 绘制一个可配置序号。

### 阶段二：拖拽与尺寸适配

- 实现鼠标拖拽。
- 实现触摸拖拽。
- 增加坐标显示和快捷对齐。
- 处理预览缩放与原图坐标转换。
- 测试横图、竖图、正方形和超宽图片。

### 阶段三：序号及样式配置

- 实现起始值、结束值和补零。
- 实现字体、颜色、透明度。
- 实现描边和阴影。
- 增加预览序号切换。

### 阶段四：批量 PNG 导出

- 实现单张 PNG 下载。
- 实现文件名模板。
- 实现批量生成和进度显示。
- 实现取消任务。
- 实现 ZIP 下载。

### 阶段五：测试与部署

- 测试不同尺寸和格式的图片。
- 测试序号范围、补零和极端输入。
- 测试移动端拖拽。
- 检查输出 PNG 的尺寸和清晰度。
- 编写 README 使用说明。
- 配置 GitHub Pages。

## 9. 验收标准

### 必须通过

- 可以上传背景图片并显示在预览区。
- 序号可以在预览区直接拖拽。
- 拖拽后的相对位置在导出图片中正确还原。
- 可以设置序号起始值和结束值。
- 可以选择是否补零。
- 可以修改字号、字体和颜色。
- 可以根据不同背景尺寸自动适配序号大小。
- 可以生成单张 PNG。
- 可以批量生成连续序号 PNG。
- 批量结果可以下载为 ZIP。
- ZIP 中的文件名和序号格式正确。
- 图片不上传到服务器。
- 项目可以直接通过 GitHub Pages 访问。

### 建议通过

- 支持描边、阴影和透明度。
- 支持触摸拖拽。
- 支持取消批量任务。
- 支持位置快捷对齐。
- 支持保存和恢复配置。
- 在手机屏幕上仍然可以正常操作。

## 10. 暂不纳入第一版的内容

- 编辑背景图片本身。
- 拆分或编辑参考图中的标题、图标、Logo。
- 多个独立文字图层。
- 图片裁剪、滤镜和调色。
- JPG、WebP 等其他输出格式。
- 用户登录、云端存储和在线项目管理。
- 服务端批量处理。

这些内容可以作为后续版本扩展，但不应影响第一版交付。

## 11. 发布与维护

- 使用 GitHub 仓库存放源代码。
- 使用 GitHub Pages 发布静态站点。
- 通过仓库中的 `README.md` 说明使用方式和浏览器要求。
- 发布前确认没有提交用户图片、个人数据、日志或密钥。
- 修改后使用真实但不敏感的测试图片验证单张和批量输出。
- 如依赖第三方库，固定版本并记录来源。

## 12. 第一版交付物

第一版应包含：

1. 可在本地直接打开或通过静态服务器运行的网页。
2. 背景图片上传功能。
3. 序号范围与补零配置。
4. 字体、字号、颜色配置。
5. 画布拖拽定位。
6. 自动适配不同原图尺寸。
7. 单张 PNG 下载。
8. 批量 PNG 生成和 ZIP 下载。
9. 响应式界面。
10. GitHub Pages 部署说明。

